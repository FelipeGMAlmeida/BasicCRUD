# Database
DB_NAME=basiccrud
DB_USER=postgres
DB_PASSWORD=postgres

# JWT
JWT_SECRET=minha-chave-secreta-super-segura-basiccrud-2026
JWT_EXPIRATION=86400000

# Frontend (URL da API acessível pelo navegador)
# Dev: http://localhost:8080 | Prod: http://<DROPLET_IP>:8080
VITE_API_URL=http://localhost:8080

# =============================================================================
# Produção — imagens Docker (geradas automaticamente pelo GitHub Actions)
# =============================================================================
# BACKEND_IMAGE=ghcr.io/<seu-usuario>/basiccrud-backend:latest
# FRONTEND_IMAGE=ghcr.io/<seu-usuario>/basiccrud-frontend:latest
